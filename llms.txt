# Project

*Documentation **v1.5** — MAJOR.MINOR (see README.md footer).*

**Messenger-Native SaaS** — Heartmade Studio reference repository. It is a **runnable, forkable starter kit** for micro-SaaS products that replace traditional browser dashboards with native messenger UX (e.g. Telegram). The system is Edge-first and centers on an **Onboarding-first flow**. The core (Telegram webhook + Supabase Postgres + a Deno Edge Function with a pure onboarding state machine) runs end-to-end; AI/billing/observability are optional adapters that degrade gracefully without env.

The reference implementation is **MoonQ** ([moonq.app](https://moonq.app)) — a Telegram bot that sends a man one daily message about his partner's menstrual-cycle phase and one concrete action. See `docs/moonq-case-study.md`.

## Core architecture stack

- **UX / front-end:** Telegram Bot API (replaces web and mobile apps).
- **Backend / compute:** Supabase Edge Functions (Deno) for low-latency event handling.
- **Database:** Supabase (PostgreSQL).
- **Subscriptions & payments:** Stripe (webhooks + hosted Checkout).
- **Intelligence (AI composition):** Gemini API (Google AI) for **personalized cues**, layered on top of deterministic business logic and reserved for paid plans. In MoonQ the cycle phase is computed by plain TypeScript (never an LLM); the 14-day free trial serves a manually reviewed, no-AI content bank, and there is no free tier.
- **Observability:** PostHog (product analytics) and Sentry (error tracking on Edge).

## Main principles (philosophy)

- **Low friction, high retention:** No passwords; no separate mobile app install.
- **Onboarding-first flow:** User data and service setup happen through a guided conversation at launch (four steps), then a free trial starts (e.g. 14 days). Payments and checkout are handled by Stripe.

This file is LLM-oriented context for the repository’s current architecture. Use these technical constraints when generating or extending features.

## Key docs

| Path | Content |
|------|---------|
| README.md | Method, Quick start, deploy, positioning |
| ARCHITECTURE.md | Diagrams and sequences |
| supabase/functions/telegram-webhook/ | Core Edge Function: dispatcher + onboarding state machine |
| supabase/functions/_shared/ | Adapters: ai (Gemini+stub), billing (Stripe skeleton), analytics (PostHog/Sentry) |
| supabase/migrations/ | Postgres schema (users + events) |
| docs/billing-and-ai.md | What's real vs TODO in the Stripe/Gemini adapters |
| docs/moonq-case-study.md | Product narrative — **MoonQ** |
| docs/security-and-compliance.md | Webhooks, secrets, sensitive data |
| DESIGN.md | Landing page tokens only |